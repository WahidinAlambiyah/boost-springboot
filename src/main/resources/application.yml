spring:
  application:
    name: boost-graphql-starter
  graphql:
    graphiql:
      enabled: true

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
