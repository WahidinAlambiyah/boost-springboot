spring:
  application:
    name: boost
  config:
    import: optional:file:.env[.properties]
  datasource:
    hikari:
      schema: ${DB_SCHEMA:fastworks_springboot_graphql}
      connection-init-sql: SET search_path TO ${DB_SCHEMA:fastworks_springboot_graphql}
      data-source-properties:
        prepareThreshold: 0
        preferQueryMode: simple
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:postgres}?currentSchema=${DB_SCHEMA:fastworks_springboot_graphql}
    username: ${DB_USER:app}
    password: ${DB_PASSWORD:app}
  graphql:
    graphiql:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: ${DB_SCHEMA:fastworks_springboot_graphql}
        jdbc:
          time_zone: UTC
    open-in-view: false
  flyway:
    enabled: true
    default-schema: ${DB_SCHEMA:fastworks_springboot_graphql}
    schemas: ${DB_SCHEMA:fastworks_springboot_graphql}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

app:
  redis:
    enabled: ${REDIS_ENABLED:false}

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info

security:
  jwt:
    issuer: boost-api
    secret: ${JWT_SECRET:dev-secret-change-me-min-32-chars}
    access-token-ttl-minutes: ${JWT_TTL_MINUTES:15}
    refresh-token-ttl-days: ${REFRESH_TTL_DAYS:30}
