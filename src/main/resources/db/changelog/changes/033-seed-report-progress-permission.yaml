databaseChangeLog:
  - changeSet:
      id: 033-seed-report-progress-permission
      author: codex
      changes:
        - sql:
            splitStatements: true
            stripComments: true
            sql: |
              insert into fastworks_springboot.permissions (code, name, description, module, is_active, created_at, updated_at)
              values ('REPORT_PROGRESS_READ', 'Report Progress Read', 'Read student progress report data', 'REPORT', true, now(), now())
              on conflict (code) do nothing;

              insert into fastworks_springboot.role_permissions (role_id, permission_id, assigned_at)
              select r.id, p.id, now()
              from fastworks_springboot.roles r
              join fastworks_springboot.permissions p on p.code = 'REPORT_PROGRESS_READ'
              where r.code in ('ADMIN', 'ROLE_ADMIN')
              on conflict (role_id, permission_id) do nothing;
