schema {
  query: Query
  mutation: Mutation
}

type Query {
  users(page: Int, size: Int, sortBy: String, sortDirection: SortDirection, filter: UserFilter): [User!]!
  user(id: ID!): User
  roles(page: Int, size: Int, sortBy: String, sortDirection: SortDirection, filter: RoleFilter): [Role!]!
  role(id: ID!): Role
  categories(page: Int, size: Int, sortBy: String, sortDirection: SortDirection, filter: CategoryFilter): [Category!]!
  category(id: ID!): Category
  products(page: Int, size: Int, sortBy: String, sortDirection: SortDirection, filter: ProductFilter): [Product!]!
  product(id: ID!): Product
}

type Mutation {
  createUser(input: CreateUserInput!): User!
  updateUser(id: ID!, input: UpdateUserInput!): User!
  deleteUser(id: ID!): Boolean!
  createRole(input: CreateRoleInput!): Role!
  updateRole(id: ID!, input: UpdateRoleInput!): Role!
  deleteRole(id: ID!): Boolean!
  createCategory(input: CreateCategoryInput!): Category!
  updateCategory(id: ID!, input: UpdateCategoryInput!): Category!
  deleteCategory(id: ID!): Boolean!
  createProduct(input: CreateProductInput!): Product!
  updateProduct(id: ID!, input: UpdateProductInput!): Product!
  deleteProduct(id: ID!): Boolean!
}

type User {
  id: ID!
  username: String!
  email: String!
  fullName: String
  roles: [Role!]!
}

type Role {
  id: ID!
  name: String!
  users: [User!]!
}

type Category {
  id: ID!
  name: String!
  parent: Category
  children: [Category!]!
}

type Product {
  id: ID!
  name: String!
  sku: String!
  price: Float!
  category: Category
}

enum SortDirection {
  ASC
  DESC
}

input CreateUserInput {
  username: String!
  email: String!
  fullName: String
  roleIds: [ID!]
}

input UpdateUserInput {
  username: String
  email: String
  fullName: String
  roleIds: [ID!]
}

input CreateRoleInput {
  name: String!
}

input UpdateRoleInput {
  name: String
}

input UserFilter {
  username: String
  email: String
  fullName: String
}

input RoleFilter {
  name: String
}

input CategoryFilter {
  name: String
  parentId: ID
}

input ProductFilter {
  name: String
  sku: String
  categoryId: ID
}

input CreateCategoryInput {
  name: String!
  parentId: ID
}

input UpdateCategoryInput {
  name: String
  parentId: ID
}

input CreateProductInput {
  name: String!
  sku: String!
  price: Float!
  categoryId: ID
}

input UpdateProductInput {
  name: String
  sku: String
  price: Float
  categoryId: ID
}
