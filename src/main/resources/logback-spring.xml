<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="LOG_PRETTY" source="LOG_PRETTY" defaultValue="false"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <if condition='property("LOG_PRETTY").equals("true")'>
            <then>
                <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                    <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} %X{correlationId} - %msg%n</pattern>
                </encoder>
            </then>
            <else>
                <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                    <includeMdcKeyName>correlationId</includeMdcKeyName>
                </encoder>
            </else>
        </if>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
